
name: spin 
help: "Start entire identity infrastructure (or part of it) and move data they need"
args:
  - name: identity
    help: "Identity for which to spin infrastructure (optional if one is active)"
    required: false 
