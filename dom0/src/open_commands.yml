
name: open
help: "Open identities and/or associated stores in the vault only"

commands:
  - name: identity
    help: "Open an identity and its associated builtin stores in the vault"
    args:
      - name: identity
        help: "Name of the identity to open"
        required: true
